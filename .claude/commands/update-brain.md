# /update-brain — Deprecated

This skill has been merged into `/dev`.

Run `/dev` instead. The Developer role automatically updates `chinese-app/docs/TECHNICAL.md` and `chinese-app/docs/REQUIREMENTS.md` as Step 4 of its workflow.

If you want to sync docs only (no code change), run `/dev` and tell Claude: "Just update the docs to match the current code — no implementation needed."
